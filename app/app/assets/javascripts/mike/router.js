Mike.Router.map(function() {
  // this.resource('top', { path: '/' });
  // this.resource('tag', { path: '/t/:tag' });
  // this.resource('show', { path: '/gif/:id' });
});

Mike.Router.reopen({
  location: 'auto'
});
